import { ThemeSpecification } from '../specification';

const fontWeights: ThemeSpecification['fontWeights'] = {
  headline: 400,
  'body-regular': 400,
  'body-bold': 700,
};

export default fontWeights;
